namespace Arrays
{
    internal class BuiltIn
    {
        // Use of some of built in array methods
        public static void Sort(int[] ar)
        {
            Array.Sort(ar);
        }
        public static void Reverse(int[] ar)
        {
            Array.Reverse(ar);
        }
    }
}
